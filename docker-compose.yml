services:
  php8phpunit:
    build:
      context: .
      dockerfile: php8phpunit.DOCKERFILE
    command:
      - bash
      - -c
      - |
        composer update
        vendor/bin/phpunit
    working_dir: /work
    volumes:
       - .:/work